# E0 Hygiene-Paket — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deploy-Leaks schließen, Smoke-Coverage auf `sections/**` ausweiten (weiche CDN-Policy), das Geld-Wert-Duplikat auflösen (Tab raus, Link rein), Doku-Drift beheben, Repo aufräumen — als sauberes Fundament vor dem Redesign (E1–E4).

**Architecture:** Ausschließlich Website-Pflege-Schicht (`assets/`, `escape-games/`, `sections/`, `index.html`, `tools/website|smoke`, `Makefile`, `CLAUDE.md`, `docs/website/`). Generator-/PM-Apparat (`docs/projekt|agents|architektur`, `bridge/`, `tools/cc-*`, `tools/deploy-check.sh` …) wird NICHT angefasst. Die `_`-Präfix-Konvention (Scratch, nie deployt/geprüft) wird als Archiv-Mechanik genutzt und im Makefile auf `assets/`+`sections/` ausgeweitet.

**Tech Stack:** vanilla HTML/CSS/JS, Python 3 (smoke.py mit Playwright), Make, git.

## Global Constraints

- Vor JEDEM Commit: `make check` muss BLOCKING-grün sein (pre-commit-Hook prüft zusätzlich).
- Explizit stagen — **nie** `git add .` / `git add -A`.
- Arbeit auf Branch `chore/e0-hygiene` (main ist Deploy-Trigger). **Kein Push, kein PR** — nur auf Ansage von Paul.
- `?v=`-Tokens nie von Hand editieren (in diesem Plan wird kein Bump nötig: geteilte Assets bleiben unverändert).
- Commits: Conventional Commits, deutsch, mit Scope (`chore(website-layer)`, `fix(wib)`, `feat(smoke)` …), aussagekräftiger Body.
- Entschiedene Vorgaben von Paul (2026-07-17): Geld-Wert-Duplikat → „Tab raus, Link rein"; Smoke-CDN-Policy → „Weich: externe Fehler = WARN, eigene = FAIL".

---

### Task 1: Branch anlegen

**Files:** keine (nur git).

- [ ] **Step 1: Sauberen Stand verifizieren**

Run: `git status --short && git rev-parse --abbrev-ref HEAD`
Expected: nur `?? 2026-05-31-…txt` (untracked), Branch `main`.

- [ ] **Step 2: Branch erstellen**

```bash
git checkout -b chore/e0-hygiene
```

---

### Task 2: Deploy-Leaks schließen (Dev-Reste + tote Assets aus dem Publish-Baum)

**Files:**
- Delete: `escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/data.json.pre-hotfix.bak`, `….pre-hotfix2.bak`, `….pre-hotfix3.bak`, `….pre-hotfix4.bak`
- Rename: `escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/` → `escape-games/_archiv-gpg-erster-weltkrieg-ursachen-run4-v050/`
- Rename: `escape-games/verlauf-erster-weltkrieg-marne-ende-diff/` → `escape-games/_dev-verlauf-erster-weltkrieg-marne-ende-diff/` (bewusst `_dev-`, nicht `_archiv-`: enthält die fertige Differenzierung, die dem Live-Pendant fehlt — Merge-Material für E2)
- Rename: `escape-games/template/` → `escape-games/_template/`
- Rename: `assets/img/gpg-erster-weltkrieg-ursachen-run4-v050/` → `assets/img/_archiv-gpg-erster-weltkrieg-ursachen-run4-v050/`
- Rename: `assets/Lehrplan_GPG7.md` → `docs/Lehrplan_GPG7.md` (unreferenzierte interne Notiz; docs/ ist nie im Deploy-Scope)
- Modify: `Makefile:51-52` (`_*/`-Exclude auch für assets- und sections-rsync)

**Interfaces:**
- Produces: `_`-Präfix gilt danach einheitlich in allen drei rsync-Zweigen von `make site`. Task 6 (Doku) referenziert die neuen Pfade `escape-games/_template/` etc.

- [ ] **Step 1: Backup-Dateien entfernen und Verzeichnisse umbenennen**

```bash
git rm escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/data.json.pre-hotfix.bak \
       escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/data.json.pre-hotfix2.bak \
       escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/data.json.pre-hotfix3.bak \
       escape-games/gpg-erster-weltkrieg-ursachen-run4-v050/data.json.pre-hotfix4.bak
git mv escape-games/gpg-erster-weltkrieg-ursachen-run4-v050 escape-games/_archiv-gpg-erster-weltkrieg-ursachen-run4-v050
git mv escape-games/verlauf-erster-weltkrieg-marne-ende-diff escape-games/_dev-verlauf-erster-weltkrieg-marne-ende-diff
git mv escape-games/template escape-games/_template
git mv assets/img/gpg-erster-weltkrieg-ursachen-run4-v050 assets/img/_archiv-gpg-erster-weltkrieg-ursachen-run4-v050
git mv assets/Lehrplan_GPG7.md docs/Lehrplan_GPG7.md
```

- [ ] **Step 2: Makefile — `_*/`-Exclude vereinheitlichen**

In `Makefile` die Zeilen

```make
	rsync -a --exclude='.DS_Store' assets _site/
	[ -d sections ] && rsync -a --exclude='.DS_Store' sections _site/ || true
```

ersetzen durch

```make
	rsync -a --exclude='_*/' --exclude='.DS_Store' assets _site/
	[ -d sections ] && rsync -a --exclude='_*/' --exclude='.DS_Store' sections _site/ || true
```

- [ ] **Step 3: Verifizieren — nichts Archiviertes im Publish-Baum, Gate grün**

```bash
make site
find _site \( -name "*run4-v050*" -o -name "*-diff*" -o -name "_template" -o -name "Lehrplan_GPG7.md" -o -name "*.bak" \) | wc -l
make check
```

Expected: `find`-Zähler `0`; `make check` → „BLOCKING-Tier GRUEN". Falls `check_assets.py`/`bump-assets.py` die `_`-Verzeichnisse doch noch anfassen (FAIL wegen nicht mehr auflösbarer `../../assets/img/gpg-…run4-v050/…`-Links): kontrollieren, dass die `_`-Segment-Ignorierung greift — beide Skripte filtern `_`-präfixierte Pfadsegmente; ein FAIL hier wäre ein echter Befund und STOPPT den Task (nicht um-hacken, sondern melden).

- [ ] **Step 4: Commit**

```bash
git add Makefile
git commit -m "chore(website-layer): Deploy-Leaks schliessen — Dev-Reste unter _-Praefix, .bak-Dateien raus, Lehrplan-Notiz nach docs/

- escape-games/{template,run4-v050,marne-ende-diff} -> _template/_archiv-/_dev- (bleiben getrackt, gehen nicht mehr live)
- 4x data.json.pre-hotfix*.bak geloescht (~650K Snapshot-Reste)
- assets/img/run4-v050 (11 MB toter Bild-Ballast) -> _archiv-Praefix
- assets/Lehrplan_GPG7.md (unreferenzierte interne Notiz) -> docs/
- make site: _*/-Exclude jetzt auch fuer assets/ und sections/"
```

(Hinweis: `git mv`/`git rm` stagen sich selbst; nur das Makefile braucht `git add`.)

---

### Task 3: Smoke-Coverage `sections/**` + weiche CDN-Policy

**Files:**
- Modify: `tools/smoke/smoke.py` (pages() + check_page() + main())

**Interfaces:**
- Consumes: Verzeichnislayout nach Task 2 (`_`-Präfix = nicht deploybar → wird auch hier ausgefiltert).
- Produces: `make smoke` testet zusätzlich alle `sections/**/*.html`; externe Subressourcen-Fehler (fremder Host, z.B. cdn.jsdelivr.net) erscheinen als `WARN`, blocken aber nicht. Eigene (same-origin) Fehler blocken weiterhin hart.

- [ ] **Step 1: `pages()` um sections erweitern**

In `tools/smoke/smoke.py` die Funktion `pages()` ersetzen durch:

```python
def pages() -> list[str]:
    """Repo-relative URL-Pfade aller zu testenden Seiten."""
    urls = ["/index.html"]
    for g in live_games():
        gdir = REPO_ROOT / "escape-games" / g
        for name in ["index.html", "lehrkraft.html"]:
            if (gdir / name).exists():
                urls.append(f"/escape-games/{g}/{name}")
        for mappe in sorted(gdir.glob("mappe-*.html")):
            urls.append(f"/escape-games/{g}/{mappe.name}")
    # sections/**: alles Deploybare (Konvention: _-praefixierte Segmente = Scratch, nie live)
    sections = REPO_ROOT / "sections"
    if sections.is_dir():
        for f in sorted(sections.rglob("*.html")):
            rel = f.relative_to(REPO_ROOT)
            if any(part.startswith("_") for part in rel.parts):
                continue
            urls.append("/" + rel.as_posix())
    return urls
```

- [ ] **Step 2: `check_page()`/`main()` — externe Fehler zu Warnungen degradieren**

`check_page` bekommt den `base`-Parameter und trennt `problems` (blockend, same-origin) von `warnings` (extern). Ersetze `check_page` und die Aufrufstelle in `main`:

```python
def check_page(page, base: str, url: str) -> tuple[list[str], list[str]]:
    problems: list[str] = []
    warnings: list[str] = []
    page.on("console", lambda m: (
        problems.append(f"console.{m.type}: {m.text}")
        if m.type == "error" and not allowed(m.text) else None))
    page.on("pageerror", lambda e: problems.append(f"pageerror: {e}"))

    def on_response(resp):
        if resp.status >= 400 and not any(sub in resp.url for sub in RESPONSE_IGNORE):
            # Weiche CDN-Policy (Paul, 2026-07-17): Fehler FREMDER Hosts blocken den
            # Deploy nicht (Dritt-Infrastruktur), same-origin-Fehler bleiben harte FAILs.
            if resp.url.startswith(base):
                problems.append(f"http {resp.status}: {resp.url}")
            else:
                warnings.append(f"http {resp.status} (extern): {resp.url}")
    page.on("response", on_response)

    resp = page.goto(url, wait_until="load", timeout=20000)
    if resp is not None and resp.status >= 400:
        problems.append(f"http {resp.status} (document): {url}")
    page.wait_for_timeout(600)  # kurzes Settle fuer late console/async render

    # Kern-DOM: jede Seite muss <main> ODER den Game-Container haben
    if page.locator("main, #game, .escape, body > *").count() == 0:
        problems.append("kein Kern-DOM-Knoten (main/#game/.escape) gefunden")
    return problems, warnings
```

In `main()` die Schleife anpassen:

```python
        for path in urls:
            page = browser.new_page()
            problems, warnings = check_page(page, base, base + path)
            page.close()
            if problems:
                fail = 1
                print(f"  FAIL  {path}")
                for pr in problems:
                    print(f"          {pr}")
            else:
                print(f"  ok    {path}")
            for w in warnings:
                print(f"  WARN  {path}: {w}")
```

Bekannte Restlücke (dokumentiert, akzeptiert): Fällt das CDN KOMPLETT aus, wirft die Seite selbst `Chart is not defined` → `pageerror` → FAIL. Das ist gewollt „echter Defekt" (die Live-Seite wäre dann auch für Schüler kaputt) — die weiche Policy fängt nur den Fall ab, dass ein externer Host 4xx/5xx liefert, ohne die Seite zu zerlegen.

- [ ] **Step 3: Lokal verifizieren**

```bash
python3 -c "import playwright" 2>/dev/null && make smoke || echo "Playwright lokal nicht installiert -> Verifikation via 'pip install playwright && python3 -m playwright install chromium' ODER als CI-Verifikation deklarieren und Paul melden"
```

Expected: `smoke: N Seiten` mit N = bisherige Zahl + 2 (haushaltsbuch.html, geldwert.html), am Ende `smoke: GRUEN`. Beide sections-Seiten `ok`.

- [ ] **Step 4: Commit**

```bash
git add tools/smoke/smoke.py
git commit -m "feat(smoke): sections/** mit-testen + weiche CDN-Policy

pages() erfasst jetzt alle deploybaren sections-Seiten (_-Praefix ausgenommen).
Subressourcen-Fehler fremder Hosts (z.B. cdn.jsdelivr.net) sind WARN statt FAIL —
same-origin-Fehler und pageerrors blocken weiter hart. Schliesst die in SITE_MAP
dokumentierte Smoke-Luecke (2 live WiB-Seiten waren bisher ungetestet)."
```

---

### Task 4: Geld-Wert-Duplikat auflösen — Tab raus, Verweis-Karte rein

**Files:**
- Modify: `sections/wib/haushaltsbuch.html` (Exzision in 8 Regionen + 1 Insert)

**Interfaces:**
- Consumes: Task 3 (Smoke deckt die Seite jetzt ab — dient als Regressionsnetz).
- Produces: Einzige Quelle des Geld-Wert-Tools ist `sections/wib/geldwert.html`. localStorage-Key `wib_haushaltsbuch_v2` behält ggf. vorhandene `geldwert`-/`gw*`-Restdaten (harmlos, wird schlicht nicht mehr gelesen).

Die Regionen sind über stabile Kommentar-Anker auffindbar (Zeilennummern = Stand vor Edit, nur zur Orientierung):

| Region | Anker | Aktion |
|---|---|---|
| CSS | `/* GELD-WERT — STEPPER */` (Z. 424) bis zum nächsten `/* …-Sektionskommentar */` | Block löschen |
| Nav | `<button data-tab="geld-wert">…NEU</span></button>` (Z. 618) | Zeile löschen |
| Section | `<!-- GELD-WERT -->` (Z. 843) bis einschließlich `</section>` vor `<!-- REFLEXION -->` (Z. 974) | Block löschen |
| JS-Daten | `// GELD-WERT DATEN` (Z. 1098) bis zum nächsten nicht-geldwert-Anker | Block löschen (`ERWACHSENEN_KAT`, `STUNDENLOEHNE`, `PERSOENLICHKEITEN` u.a.) |
| Restore | `// Geld-Wert-Restore` inkl. `if (daten.geldwert) {…}` (Z. 1219–1236) | Block löschen (umgebende Lade-Funktion bleibt) |
| State | `// GELD-WERT STATE` (Z. 1350) Block | löschen |
| Init-Aufrufe | `// === Geld-Wert: 3 Charts + Sliders + Step-Nav ===` + die 8 Folgezeilen `initSteppNavigation();` … `initEigenLohnInput();` (Z. 1453–1461) | löschen (umgebende Init-Funktion bleibt) |
| Funktionen | `// GELD-WERT — STEP NAVIGATION` (Z. 1465) bis einschließlich Ende von `aktualisiereGeldWertCharts()` (beginnt Z. 1739) | Block löschen (`initSteppNavigation`, `initBudgetSlider`, `resetBudget`, `initLohnButtons`, `initEigenLohnInput`, `initBudgetChart`, `initArbeitChart`, `initPersoenlichkeitButtons`, `initReicherChart`, `aktualisiereGeldWertCharts` — geteilte Utilities wie `autoSave`, `zeigeToast`, `fmtEUR`, `macheStackedChart` NICHT löschen) |
| Save | Zeile `daten.geldwert = { budget: mein_budget, lohn: aktuellerLohn, person: aktivePerson.id };` (Z. 1815, innerhalb der Speicher-Funktion) | Zeile löschen |

- [ ] **Step 1: Exzision durchführen** (Anker-Tabelle oben; vor jedem Löschblock die Umgebung lesen, damit geteilte Funktionen intakt bleiben)

- [ ] **Step 2: Verweis-Karte einfügen** — ans Ende von `<section id="vergleich">` (unmittelbar vor dessen `</section>`):

```html
  <!-- VERWEIS: Geld-Wert-Tool (eigenstaendige Seite, ersetzt den frueheren Tab) -->
  <div class="karte" style="margin-top:1.5rem">
    <h3>Weiter: Was ist mein Geld wert?</h3>
    <p>Drei Schritte — Budget mit Slidern, dein Wunschberuf in Arbeitsstunden,
       und was Reiche für dasselbe Budget arbeiten müssten.</p>
    <div class="aktion-buttons">
      <a href="geldwert.html"><button type="button">→ Zum Geld-Wert-Tool</button></a>
    </div>
  </div>
```

- [ ] **Step 3: Keine Waisen-Referenzen**

```bash
grep -c -iE "geld-?wert|initBudget|initLohn|initReicher|initArbeit|initSteppNav|initPersoenlichkeit|initEigenLohn|resetBudget|ERWACHSENEN_KAT|STUNDENLOEHNE|PERSOENLICHKEITEN|mein_budget|aktuellerLohn|aktivePerson|chart-budget|chart-arbeit|chart-reicher|step-nav|step-content|data-reflex=\"gw" sections/wib/haushaltsbuch.html
```

Expected: `1` (einzig der Kommentar der neuen Verweis-Karte matcht „Geld-Wert"). Jeder weitere Treffer = vergessener Rest → nacharbeiten.

- [ ] **Step 4: Render-Verifikation**

```bash
make check && make smoke
```

Expected: beides grün; `haushaltsbuch.html` `ok` (kein pageerror durch entfernte Funktionen), `geldwert.html` `ok`. Zusätzlich manuell (lokaler Server): Tab-Leiste zeigt 5 Tabs, Vergleich-Tab endet mit der Verweis-Karte, Klick führt zu `geldwert.html`.

- [ ] **Step 5: Commit**

```bash
git add sections/wib/haushaltsbuch.html
git commit -m "fix(wib): Geld-Wert-Duplikat aufgeloest — Tab raus, Verweis auf geldwert.html rein

Das Tool existierte doppelt (Tab im Haushaltsbuch + eigenstaendige Seite) mit
divergenten Daten (u.a. Polizist/in 15 vs. 20 EUR/h) und getrennten
localStorage-Keys. Einzige Quelle ist jetzt sections/wib/geldwert.html
(die reichere Fassung). Entscheidung Paul 2026-07-17."
```

---

### Task 5: Doku-Drift beheben (SITE_MAP + CLAUDE.md)

**Files:**
- Modify: `docs/website/SITE_MAP.md`
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: neue Pfade aus Task 2, Smoke-Stand aus Task 3, WiB-Stand aus Task 4.

- [ ] **Step 1: SITE_MAP.md aktualisieren** — fünf Punkte:

1. Verticals-Tabelle, Zeile Escape-Games: Template-Pfad `escape-games/template/` → `escape-games/_template/`; Status „live (3 Games)" → „live (4 Games)".
2. Verticals-Tabelle, Zeile WiB: „live (1 Tool: Haushaltsbuch)" → „live (2 Tools: Haushaltsbuch, Geld-Wert — Geld-Wert nur noch als eigenständige Seite, Duplikat-Tab am 17.07. entfernt)".
3. Live-Games-Liste: `syrische-revolution-2011 — Syrische Revolution 2011 (GPG R7)` ergänzen, mit Hinweis: „⚠ nutzt eigene `vendor/`-Runtime (Engine-Fork, ohne `?v=`-Governance) — Rückführung geplant (E2, s. Redesign-Prozess)".
4. Absatz „Nicht-verlinkte Game-Verzeichnisse": neue Namen `_template/`, `_archiv-gpg-erster-weltkrieg-ursachen-run4-v050/`, `_dev-verlauf-erster-weltkrieg-marne-ende-diff/` + Satz „per `_`-Präfix aus Deploy/Checks ausgenommen; `_dev-…-diff` enthält die Differenzierung als Merge-Material für das Live-Pendant".
5. „Offene IA-Fragen": Smoke-Coverage-Punkt (Z. 54–58) ersetzen durch: „~~Smoke-Coverage sections~~ erledigt 2026-07-17: `smoke.py` testet `sections/**` mit; externe CDN-Fehler = WARN (weiche Policy), same-origin = FAIL."
6. Stand-Datum im Kopf: „Stand: 2026-06-07" → „Stand: 2026-07-17".

- [ ] **Step 2: CLAUDE.md aktualisieren** — zwei Sätze:

1. Schnellreferenz, Zeile `make smoke`: „…aller Live-Seiten" → „…aller Live-Seiten (Games + sections)".
2. Abschnitt „Geteilte Runtime": nach dem Satz über `escape-engine.js` ergänzen: „**Ausnahme (Stand 07/2026):** `syrische-revolution-2011` bindet eine eigene `vendor/`-Kopie der Runtime ein (Engine-Fork, ohne `?v=`) — bis zur Rückführung (E2) erreichen Engine-Bumps dieses Game NICHT."

- [ ] **Step 3: Verifizieren + Commit**

```bash
make check
git add docs/website/SITE_MAP.md CLAUDE.md
git commit -m "docs(website-layer): SITE_MAP + CLAUDE.md auf Ist-Stand 2026-07-17

4 Live-Games inkl. syrische-revolution-2011 (vendor-Ausnahme markiert),
WiB = 2 Tools (Duplikat aufgeloest), _-Archiv-Pfade, Smoke-Luecke als
geschlossen dokumentiert."
```

---

### Task 6: Repo-Ordnung (Root-Transkript, lokale Branches) + Abschluss-Gate

**Files:**
- Move (untracked, kein git): `2026-05-31-170009-this-session-is-being-continued-from-a-previous-c.txt` → `docs/analyse/session-export-2026-05-31/` (Pfadmuster `docs/analyse/session-export-*/` ist gitignored — Datei bleibt auffindbar, Root wird sauber, nichts landet in git)

- [ ] **Step 1: Transkript verschieben**

```bash
mkdir -p docs/analyse/session-export-2026-05-31
mv 2026-05-31-170009-this-session-is-being-continued-from-a-previous-c.txt docs/analyse/session-export-2026-05-31/
git status --short
```

Expected: `git status` zeigt KEINE neuen Einträge (Zielpfad ist ignoriert), Root-Untracked weg.

- [ ] **Step 2: Gemergten lokalen Branch löschen**

```bash
git branch -d feat/website-maintenance-layer
```

Expected: „Deleted branch …". (`wave-1-engine-patches` und `claude/silly-shirley` NICHT anfassen — ungemergt, fachfremd bzw. Generator-Welt.)

- [ ] **Step 3: Abschluss-Gate über den ganzen Branch**

```bash
make check && make smoke && make site && du -sh _site
```

Expected: check grün, smoke grün (inkl. 2 sections-Seiten), `_site` spürbar kleiner als zuvor (~12 MB weniger: 11 MB img-Ballast + ~1 MB Dev-Reste).

- [ ] **Step 4: Abschluss-Meldung an Paul** (KEIN Push!)

Zu melden: Branch `chore/e0-hygiene` mit 4 Commits fertig + Gate-Beweise. Offene Ansage-Punkte für Paul:
1. Push/Merge des Branches (Deploy-Trigger).
2. Optional Remote-Aufräumen (separat, außerhalb dieses Branches): `git push origin --delete feat/marne-diff-v2 fix/marne-diff-frage6 fix/marne-diff-kiprompt fix/marne-diff-meta-flag fix/mappe2-quality-patches skript-redesign-v2-werkbank` — 6 nachweislich in main enthaltene Alt-Branches.

---

## Self-Review (durchgeführt 2026-07-17)

- **Spec-Coverage:** Deploy-Leaks (Task 2) ✓ · Smoke-Lücke (Task 3) ✓ · Geld-Wert (Task 4, Entscheidung „Tab raus") ✓ · Doku-Drift (Task 5) ✓ · Aufräumen Root/Branches (Task 6) ✓. NICHT in E0 (bewusst): Engine-Fork-Rückführung + `?v=`-Governance des Syrien-Games (E2), Favicon (E3), toter `tools/pre-commit-atom-check.sh` + `tools/lernraum/`-Waisen (Generator-nah bzw. ruhend — nur dokumentiert, nicht angefasst).
- **Platzhalter-Scan:** Task 4 arbeitet mit Anker-Tabelle + Grep-Postcondition statt Verbatim-Diff (1964-Zeilen-Datei; Anker sind stabiler als Zeilennummern) — die Postcondition (Step 3, Expected `1`) macht den Task trotzdem objektiv prüfbar.
- **Konsistenz:** `_`-Präfix-Namen in Task 2 = Namen in Task 5 (SITE_MAP) ✓ · `check_page(page, base, url)`-Signatur = Aufrufstelle in `main()` ✓.
